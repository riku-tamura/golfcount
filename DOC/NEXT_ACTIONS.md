# Next Actions

## まず確認したほうがいいこと

優先度高:

1. 実機の Apple Watch で `+1 / -1` が押しやすいか
2. 長押し連続操作の速度が速すぎないか
3. `次のホールへ` の動きが想定どおりか
4. アプリを終了して再起動しても値が保持されるか
5. 明るい屋外でも数字とボタン色が見やすいか

## 今回こちらで実行したこと

- `次のホールへ` ボタンを追加
  - ホール番号を進める
  - `打数 / ペナルティ / パット` を0に戻す
- `1つ戻す` ボタンを追加
  - 直前のホール移動だけ取り消せる
  - 次の入力を始めたら戻し操作は無効化する
- 触覚フィードバックを追加
  - `+1 / -1` は `click`
  - `次のホールへ` は `success`
  - `1つ戻す` は `directionDown`
- アクセシビリティ文言を追加
  - VoiceOver で各ボタンの意味が伝わるようにした

## 次にやったほうがいいこと

### 優先度高

- ホールごとの履歴を持つかどうか決める
  - 今は「現在のホールのメモ」に特化している
  - 後から見返したいなら履歴機能が必要
- `次のホールへ` の仕様確認
  - 現在は確認なしで進む
  - 直後なら `1つ戻す` で復帰できる
  - さらに厳密にしたいなら確認ダイアログを追加する

### 優先度中

- パー入力の追加
  - スコア比較がしやすくなる
- メモ欄の追加
  - フェアウェイキープやバンカーなどを残せる
- Digital Crown 操作の対応
  - タップ以外の入力手段を増やせる

### 優先度低

- ラウンド履歴一覧
- エクスポート
- 統計表示

## おすすめの次の進め方

1. 実機で 9ホール分ほど触って、誤タップや操作回数の多さを確認する
2. `履歴が必要か / いらないか` を決める
3. 履歴が不要なら `パー` と `メモ` を足す
4. 履歴が必要なら、次はデータ構造を `1ホール単位` に拡張する
