# Next Actions

## まず確認したほうがいいこと

優先度高:

1. 実機の Apple Watch で `+1 / -1` が押しやすいか
2. 長押し連続操作の速度が速すぎないか
3. `次のホールへ` の動きが想定どおりか
4. アプリを終了して再起動しても値が保持されるか
5. 明るい屋外でも数字とボタン色が見やすいか

## 今回こちらで実行したこと

- 18ホール分の状態を保持するように変更
- 最上部を18ホール合計スコアだけのシンプルな表示に変更
- `打数 / パット / ペナルティ` の3項目だけを残す形に整理
- `1H〜18H` を直接選べるホール選択 UI を追加
- 触覚フィードバックを追加
  - `+1 / -1` は `click`
  - ホール選択も `click`
- アクセシビリティ文言を追加
  - VoiceOver で各ボタンの意味が伝わるようにした

## 次にやったほうがいいこと

### 優先度高

- 実機でホール選択 UI の押しやすさを確認する
- 18ホール合計の見え方が屋外でも十分か確認する

### 優先度中

- パー入力の追加
  - スコア比較がしやすくなる
- メモ欄の追加
  - フェアウェイキープやバンカーなどを残せる
- Digital Crown 操作の対応
  - タップ以外の入力手段を増やせる

### 優先度低

- ラウンド履歴一覧
- エクスポート
- 統計表示

## おすすめの次の進め方

1. 実機で 9ホール分ほど触って、ホール選択と入力の切り替えが速いか確認する
2. `パー` が必要かどうかを決める
3. 必要なら次に `パー` を各ホールへ追加する
4. その後に `メモ` や統計表示を検討する
